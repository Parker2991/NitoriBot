yeah i know theres plugins and modules now but i didnt know what else to name it but
modules are for files loaded from the bot array and plugins are for files needed to be loaded outside the bot array as a lazy fix
