# ignore node_modules
node_modules

# ignore build folder
build

# ignore config
config.toml

# ignore .git folder
.git

# ignore the .gradle folder in the debug bridge
debug-bridge/.gradle

# ignore the build folder in the debug bridge
debug-bridge/build
